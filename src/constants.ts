// Number of messages to keep in memory/database
export const DEFAULT_HISTORY_SIZE = 200;

// Number of messages to actually render in the UI
// Keeping this smaller improves performance and reduces flicker
export const RENDER_WINDOW_SIZE = 100;
