# Arize Phoenix Environment Variables
# Copy this file to .env and configure for your environment

# ============================================================================
# Server Configuration
# ============================================================================

# The host to run the Phoenix server (defaults to 0.0.0.0)
PHOENIX_HOST=0.0.0.0

# The port to run the Phoenix web UI (defaults to 6006)
PHOENIX_PORT=6006

# The gRPC port for OTEL trace ingestion (defaults to 4317)
PHOENIX_GRPC_PORT=4317

# The root path prefix for running behind a reverse proxy
# PHOENIX_HOST_ROOT_PATH=/phoenix

# ============================================================================
# Storage Configuration
# ============================================================================

# Working directory for SQLite database and data files (defaults to ~/.phoenix/)
PHOENIX_WORKING_DIR=/phoenix-data

# PostgreSQL database URL (for production deployments)
# Format: postgresql://user:password@host:port/database
# PHOENIX_SQL_DATABASE_URL=postgresql://phoenix:phoenix@postgres:5432/phoenix

# Alternative PostgreSQL configuration (individual variables)
# PHOENIX_POSTGRES_HOST=postgres
# PHOENIX_POSTGRES_PORT=5432
# PHOENIX_POSTGRES_USER=phoenix
# PHOENIX_POSTGRES_PASSWORD=phoenix
# PHOENIX_POSTGRES_DB=phoenix

# ============================================================================
# Security & Authentication
# ============================================================================

# Admin users (semicolon-separated list of username=email pairs)
# PHOENIX_ADMINS=admin=admin@example.com;user2=user2@example.com

# OAuth2 / OIDC configuration (see Arize docs for provider-specific setup)
# PHOENIX_AUTH_PROVIDER=oidc
# PHOENIX_OIDC_CLIENT_ID=your-client-id
# PHOENIX_OIDC_CLIENT_SECRET=your-client-secret
# PHOENIX_OIDC_ISSUER=https://your-issuer.com

# ============================================================================
# Features & External Resources
# ============================================================================

# Allow external resources like Google Fonts (defaults to true)
# Set to false for air-gapped environments
PHOENIX_ALLOW_EXTERNAL_RESOURCES=true

# ============================================================================
# Observability
# ============================================================================

# Enable Prometheus metrics endpoint on port 9090
# PHOENIX_ENABLE_PROMETHEUS=true
